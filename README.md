# News-Paper-Scraper

This repository has been cleared to make room for a new project.

The previous site — a personalized news feed (GitHub Pages frontend + Flask/TF-IDF
backend on an Azure VM) — is preserved in full at tag **`archive/v1`**:

    git checkout archive/v1

The backend (Flask API, RSS fetchers, and the JSON article archive) never lived in
this repo and was archived off-VM on 2026-09-03.
